- After providing `Username and Password` user can select which `Role` he/she will log into.

-  Fields:
    - `Username` : `String`
    - `Password` : `String`
    - `Role` : `String` (Admin, User, Guest)